1.基本创建

# 方法1：小括号（推荐）
t1 = (1, 2, 3)
# 方法2：省略括号（逗号是关键）
t2 = 1, 2, 3   # 也是元组
# 方法3：从列表转换
lst = [1, 2, 3]
t3 = tuple(lst)  # (1, 2, 3)
# 方法4：空元组
t4 = ()

2.嵌套取值


# 元组里套字典
data = ("小明", {"age": 18, "city": "北京"})
# 分步取值（推荐，清晰）
name = data[0]          # "小明"
info = data[1]          # {"age": 18, "city": "北京"}
age = info["age"]       # 18
city = info["city"]     # "北京"
# 链式取值（简洁）
age = data[1]["age"]    # 18

3.使用

# 1. 坐标点（固定不变）
point = (10, 20)
# 2. 函数的多个返回值（你的笔记知识点5）
def get_stats(nums):
    return sum(nums), len(nums), sum(nums)/len(nums)  # 返回元组
# 3. 字典的键（列表不能做键，元组可以）
d = {(1, 2): "坐标点"}  # ✅ 元组可以做键

# 4. 数据记录（固定字段）
student = ("张三", 18, "北京")
